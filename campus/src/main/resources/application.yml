#端口配置
server:
  port: 8080
#jdbc配置
spring:
  application:
    name: campus
  datasource:
    #name: campus_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/campus_db?useUnicode=true&characterEncoding=utf8
    username: root
    password: 123456
  #mybatis配置
mybatis:
  #实体类所在包名
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.example.bookDeal.dao
